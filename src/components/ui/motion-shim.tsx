'use client';

// Export high-performance native Framer Motion components & hooks
export {
  motion,
  AnimatePresence,
  useScroll,
  useTransform,
  useSpring,
  useInView,
  useAnimation,
} from 'framer-motion';

export type {
  HTMLMotionProps,
  AnimationProps,
  Variants,
  Transition,
} from 'framer-motion';
